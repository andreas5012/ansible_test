- name: create ansible user
  user:
    name: ansible
    system: yes

- name: copy sudoers_ansible
  copy:
    src: files/sudoers_ansible
    dest: /etc/sudoers.d/ansible
    owner: root
    group: root
    mode: 0440

- name: create user andreas
  user:
    name: andreas
    groups: users, admin
    append: yes
    password: RPA2K5hiF/Wyc
